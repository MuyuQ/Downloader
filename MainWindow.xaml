<Window x:Class="WYDownloader.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WYDownloader"
        mc:Ignorable="d"
        Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResize"
        Icon="ico.ico">

    <Window.Resources>
        <!-- 阴影效果 -->
        <DropShadowEffect x:Key="DropShadowEffect" Color="Black" Direction="270" ShadowDepth="2" Opacity="0.2" BlurRadius="4"/>

        <!-- 按钮样式 -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#FF4A90E2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF357ABD"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF2E6DA4"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#FFCCCCCC"/>
                                <Setter Property="Foreground" Value="#FF666666"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 文本框样式 -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="#FFDDDDDD"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                        Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#FF4A90E2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 进度条样式 -->
        <Style x:Key="ModernProgressBar" TargetType="ProgressBar">
            <Setter Property="Height" Value="8"/>
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="Foreground" Value="#3498DB"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <Grid>
                                <Border Name="PART_Track" />
                                <Border Name="PART_Indicator"
                                        Background="{TemplateBinding Foreground}"
                                        HorizontalAlignment="Left"
                                        CornerRadius="4"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- 主容器 -->
    <Border CornerRadius="15">
        <Border.Effect>
            <DropShadowEffect Color="Black" Direction="315" ShadowDepth="8" Opacity="0.3" BlurRadius="15"/>
        </Border.Effect>

        <!-- 整个界面的背景图片 -->
        <Border CornerRadius="15">
            <Border.Background>
                <ImageBrush x:Name="BackgroundImageBrush" Stretch="UniformToFill"/>
            </Border.Background>

            <!-- 半透明遮罩层 -->
            <Border Background="#77000000" CornerRadius="15">
                <Grid>
                    <Grid.RowDefinitions>
                        <!-- 上方：公告区域（扩大高度） -->
                        <RowDefinition Height="204*"/>
                        <RowDefinition Height="205*"/>
                        <!-- 中上：独立的进度条区域 -->
                        <RowDefinition Height="Auto" MinHeight="92.78"/>
                        <!-- 下方：下载控件区域 -->
                        <RowDefinition Height="Auto" MinHeight="100"/>
                    </Grid.RowDefinitions>

                    <!-- 窗口控制按钮组 -->

                    <!-- 上方：公告区域（扩大高度） -->
                    <Grid Grid.Row="0" Margin="20,60,20,20" Grid.RowSpan="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="227*"/>
                            <RowDefinition Height="100*"/>
                        </Grid.RowDefinitions>
                        <!-- 公告区域背景 -->
                        <Border CornerRadius="15" Margin="0,-16,0,-69" Grid.RowSpan="2">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                    <GradientStop Color="#30000000" Offset="0"/>
                                    <GradientStop Color="#20000000" Offset="0.5"/>
                                    <GradientStop Color="#30000000" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.Effect>
                                <DropShadowEffect Color="Black" Direction="270" ShadowDepth="3" Opacity="0.3" BlurRadius="8"/>
                            </Border.Effect>

                            <ScrollViewer VerticalScrollBarVisibility="Auto"
                                        HorizontalScrollBarVisibility="Disabled"
                                        Padding="35,25">
                                <ScrollViewer.Resources>
                                    <!-- 自定义滚动条样式 -->
                                    <Style TargetType="ScrollBar">
                                        <Setter Property="Width" Value="8"/>
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ScrollBar">
                                                    <Grid>
                                                        <Border Background="#20FFFFFF" CornerRadius="4"/>
                                                        <Track Name="PART_Track" IsDirectionReversed="True">
                                                            <Track.Thumb>
                                                                <Thumb>
                                                                    <Thumb.Template>
                                                                        <ControlTemplate TargetType="Thumb">
                                                                            <Border Background="#60FFFFFF" CornerRadius="4"/>
                                                                        </ControlTemplate>
                                                                    </Thumb.Template>
                                                                </Thumb>
                                                            </Track.Thumb>
                                                        </Track>
                                                    </Grid>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ScrollViewer.Resources>

                                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <!-- 公告标题区域 -->

                                    <!-- 公告内容区域 -->
                                    <!-- 公告标题区域 -->

                                    <!-- 公告内容区域 -->
                                    <StackPanel x:Name="txtAnnouncementContent"
                                               HorizontalAlignment="Center"
                                               MaxWidth="500">
                                        <!-- 动态加载公告内容 -->
                                    </StackPanel>
                                </StackPanel>
                            </ScrollViewer>
                        </Border>
                        <TextBlock x:Name="txtAnnouncementTitle"
                                                     Text="WY直链下载器"
                                                     FontSize="22"
                                                     FontWeight="Bold"
                                                     Foreground="#FFE8F4FD"
                                                     VerticalAlignment="Top" Margin="0,-52,642,0" FontFamily="MV Boli"/>
                    </Grid>

                    <!-- 中上：独立的进度条区域 -->
                    <Grid Grid.Row="2" Margin="20,10,20,15"/>

                    <!-- 下方：下载控件区域 -->
                    <Grid Grid.Row="3" Margin="10,30,10,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- 左侧：自动解压勾选框 -->
                        <Border Grid.Column="0"
                                Background="#33FFFFFF"
                                CornerRadius="8"
                                Margin="10,5,5,5"
                                Height="50"
                                Padding="15">
                            <CheckBox x:Name="chkAutoExtract"
                                     Content="自动解压ZIP"
                                     FontSize="13"
                                     IsChecked="True"
                                     Foreground="White"
                                     VerticalAlignment="Center"
                                     HorizontalAlignment="Center">
                                <CheckBox.Style>
                                    <Style TargetType="CheckBox">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="CheckBox">
                                                    <StackPanel Orientation="Horizontal">
                                                        <Border x:Name="CheckBoxBorder"
                                                               Width="18"
                                                               Height="18"
                                                               Background="White"
                                                               BorderBrush="#CCCCCC"
                                                               BorderThickness="2"
                                                               CornerRadius="3"
                                                               Margin="0,0,8,0">
                                                            <Path x:Name="CheckMark"
                                                                 Data="M 2 6 L 6 10 L 14 2"
                                                                 Stroke="#4CAF50"
                                                                 StrokeThickness="2"
                                                                 Visibility="Collapsed"/>
                                                        </Border>
                                                        <ContentPresenter VerticalAlignment="Center"/>
                                                    </StackPanel>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsChecked" Value="True">
                                                            <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                                            <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="#4CAF50"/>
                                                        </Trigger>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="#4CAF50"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </CheckBox.Style>
                            </CheckBox>
                        </Border>

                        <!-- 中间：下载链接选择下拉框 -->
                        <Border Grid.Column="1"
                                Background="#33FFFFFF"
                                CornerRadius="8"
                                Margin="5"
                                Height="50"
                                Padding="15">
                            <ComboBox x:Name="cmbDownloadLinks"
                                     FontSize="14"
                                     Background="White"
                                     Foreground="#333333"
                                     BorderBrush="#DDDDDD"
                                     BorderThickness="1"
                                     SelectionChanged="CmbDownloadLinks_SelectionChanged" Margin="0,-5,0,-5">
                                <ComboBox.Style>
                                    <Style TargetType="ComboBox">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ComboBox">
                                                    <Grid>
                                                        <ToggleButton Name="ToggleButton"
                                                                     Background="{TemplateBinding Background}"
                                                                     BorderBrush="{TemplateBinding BorderBrush}"
                                                                     BorderThickness="{TemplateBinding BorderThickness}"
                                                                     Focusable="False"
                                                                     IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                                     ClickMode="Press">
                                                            <ToggleButton.Template>
                                                                <ControlTemplate TargetType="ToggleButton">
                                                                    <Border Background="{TemplateBinding Background}"
                                                                           BorderBrush="{TemplateBinding BorderBrush}"
                                                                           BorderThickness="{TemplateBinding BorderThickness}"
                                                                           CornerRadius="6">
                                                                        <Grid>
                                                                            <Grid.ColumnDefinitions>
                                                                                <ColumnDefinition Width="*"/>
                                                                                <ColumnDefinition Width="20"/>
                                                                            </Grid.ColumnDefinitions>
                                                                            <Path Grid.Column="1"
                                                                                 HorizontalAlignment="Center"
                                                                                 VerticalAlignment="Center"
                                                                                 Data="M 0 0 L 4 4 L 8 0 Z"
                                                                                 Fill="#666666"/>
                                                                        </Grid>
                                                                    </Border>
                                                                    <ControlTemplate.Triggers>
                                                                        <Trigger Property="IsMouseOver" Value="True">
                                                                            <Setter Property="BorderBrush" Value="#4CAF50"/>
                                                                        </Trigger>
                                                                    </ControlTemplate.Triggers>
                                                                </ControlTemplate>
                                                            </ToggleButton.Template>
                                                        </ToggleButton>
                                                        <ContentPresenter Name="ContentSite"
                                                                         IsHitTestVisible="False"
                                                                         Content="{TemplateBinding SelectionBoxItem}"
                                                                         ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                                         ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                                         Margin="12,0,25,0"
                                                                         VerticalAlignment="Center"
                                                                         HorizontalAlignment="Left"/>
                                                        <Popup Name="Popup"
                                                              Placement="Bottom"
                                                              IsOpen="{TemplateBinding IsDropDownOpen}"
                                                              AllowsTransparency="True"
                                                              Focusable="False"
                                                              PopupAnimation="Slide">
                                                            <Grid Name="DropDown"
                                                                 SnapsToDevicePixels="True"
                                                                 MinWidth="{TemplateBinding ActualWidth}"
                                                                 MaxHeight="200">
                                                                <Border Background="White"
                                                                       BorderBrush="#DDDDDD"
                                                                       BorderThickness="1"
                                                                       CornerRadius="6"
                                                                       Effect="{StaticResource DropShadowEffect}"/>
                                                                <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                                                    <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                                                </ScrollViewer>
                                                            </Grid>
                                                        </Popup>
                                                    </Grid>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ComboBox.Style>
                                <ComboBox.ItemContainerStyle>
                                    <Style TargetType="ComboBoxItem">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="Foreground" Value="#333333"/>
                                        <Setter Property="Padding" Value="12,8"/>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#F0F0F0"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter Property="Background" Value="#4CAF50"/>
                                                <Setter Property="Foreground" Value="White"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </ComboBox.ItemContainerStyle>
                            </ComboBox>
                        </Border>

                        <!-- 右侧：开始下载按钮 -->
                        <Border Grid.Column="2"
                                Background="#33FFFFFF"
                                CornerRadius="8"
                                Margin="5,5,10,5"
                                Height="50"
                                Padding="15">
                            <Button x:Name="btnDownload"
                                    Background="#4CAF50"
                                    BorderThickness="0"
                                    Foreground="White"
                                    FontSize="14"
                                    FontWeight="SemiBold"
                                    Content="开始下载"
                                    Cursor="Hand"
                                    Click="BtnDownload_Click" Margin="0,-8,0,-8">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="6"
                                                BorderThickness="{TemplateBinding BorderThickness}">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#45A049"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter Property="Background" Value="#3E8E41"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </Border>
                    </Grid>
                    <StackPanel Grid.Row="0"
                               Orientation="Horizontal"
                               Margin="808,10,10,165">

                        <!-- 最小化按钮 -->
                        <Button x:Name="btnMinimize"
                                Width="24"
                                Height="24"
                                Background="#95A5A6"
                                BorderThickness="0"
                                Foreground="White"
                                FontSize="14"
                                FontWeight="Bold"
                                Content="−"
                                Cursor="Hand"
                                Margin="2,0"
                                Click="BtnMinimize_Click">
                            <Button.Template>
                                <ControlTemplate TargetType="{x:Type Button}">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="4"
                                            BorderThickness="{TemplateBinding BorderThickness}">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#7F8C8D"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <!-- 最大化/恢复按钮 -->
                        <Button x:Name="btnMaximize"
                                Width="24"
                                Height="24"
                                Background="#3498DB"
                                BorderThickness="0"
                                Foreground="White"
                                FontSize="12"
                                FontWeight="Bold"
                                Content="□"
                                Cursor="Hand"
                                Margin="2,0"
                                Click="BtnMaximize_Click">
                            <Button.Template>
                                <ControlTemplate TargetType="{x:Type Button}">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="4"
                                            BorderThickness="{TemplateBinding BorderThickness}">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#2980B9"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                        <Button x:Name="btnClose"
                                Width="24"
                                Height="24"
                                Background="#E74C3C"
                                BorderThickness="0"
                                Foreground="White"
                                FontSize="14"
                                FontWeight="Bold"
                                Content="×"
                                Cursor="Hand"
                                Click="BtnClose_Click">
                            <Button.Template>
                                <ControlTemplate TargetType="{x:Type Button}">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="4"
                                            BorderThickness="{TemplateBinding BorderThickness}">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#C0392B"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <!-- 关闭按钮 -->
                    </StackPanel>

                    <Border Background="#33FFFFFF"
                                CornerRadius="8"
                                Padding="20,15" Margin="20,55,20,0" Height="68" VerticalAlignment="Top" Grid.RowSpan="2" Grid.Row="2">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- 左侧：进度条区域 -->
                            <StackPanel Grid.Column="0" Margin="0,0,15,0">
                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock x:Name="lblProgress"
                                              Grid.Column="0"
                                              Text="准备就绪"
                                              FontSize="14"
                                              Foreground="White"
                                              FontWeight="SemiBold"/>
                                    <TextBlock x:Name="lblSpeed"
                                              Grid.Column="1"
                                              Text=""
                                              FontSize="14"
                                              Foreground="#E8E8E8"/>
                                </Grid>
                                <ProgressBar x:Name="progressBar"
                                            Style="{StaticResource ModernProgressBar}"
                                            Value="0"
                                            Maximum="100"
                                            Height="12"
                                            Background="#44FFFFFF"
                                            Foreground="#4CAF50"/>
                            </StackPanel>

                            <!-- 右侧：暂停/继续按钮 -->
                            <Button x:Name="btnPauseResume"
                                    Grid.Column="1"
                                    Width="60"
                                    Background="#FF9800"
                                    BorderThickness="0"
                                    Cursor="Hand"
                                    IsEnabled="False"
                                    Click="BtnPauseResume_Click" Margin="0,0,0,-12">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="6"
                                                BorderThickness="{TemplateBinding BorderThickness}">
                                            <Grid>
                                                <!-- 暂停图标 (默认) -->
                                                <Path x:Name="PauseIcon"
                                                      Fill="White"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"
                                                      Data="M6,4 L10,4 L10,20 L6,20 Z M14,4 L18,4 L18,20 L14,20 Z"
                                                      Stretch="Uniform"
                                                      Width="16"
                                                      Height="16"/>
                                                <!-- 播放图标 (隐藏) -->
                                                <Path x:Name="PlayIcon"
                                                      Fill="White"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"
                                                      Data="M8,5.14V19.14L19,12.14L8,5.14Z"
                                                      Stretch="Uniform"
                                                      Width="16"
                                                      Height="16"
                                                      Visibility="Collapsed"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#F57C00"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter Property="Background" Value="#E65100"/>
                                            </Trigger>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter Property="Background" Value="#CCCCCC"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </Grid>
                    </Border>


                </Grid>
            </Border>
        </Border>
    </Border>
</Window>
