<Window x:Class="WYDownloader.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="直链下载器"
        Height="600" Width="850"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResizeWithGrip"
        Icon="Resources/Icons/ico.ico">

    <Window.Resources>
        <!-- 主按钮样式 -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Width" Value="120"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#3B82F6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2563EB"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1D4ED8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 图标按钮样式 -->
        <Style x:Key="IconButton" TargetType="Button">
            <Setter Property="Width" Value="36"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#9CA3AF"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1AFFFFFF"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 卡片样式 -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="#0DFFFFFF"/>
            <Setter Property="BorderBrush" Value="#1AFFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
        </Style>

        <!-- ComboBox样式 -->
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Background" Value="#1AFFFFFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#33FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <Grid>
                                <ToggleButton x:Name="ToggleButton"
                                             Background="Transparent"
                                             BorderThickness="0"
                                             IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="40"/>
                                                </Grid.ColumnDefinitions>
                                                <ContentPresenter Grid.Column="0" 
                                                                Content="{Binding Path=SelectionBoxItem, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                                Margin="15,0,0,0"
                                                                VerticalAlignment="Center"/>
                                                <Path Grid.Column="1" 
                                                      Data="M 0 0 L 5 5 L 10 0"
                                                      Fill="#80FFFFFF"
                                                      StrokeThickness="0"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"/>
                                            </Grid>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                                <Popup x:Name="Popup"
                                       Placement="Bottom"
                                       IsOpen="{TemplateBinding IsDropDownOpen}"
                                       AllowsTransparency="True">
                                    <Border Background="#374151"
                                            BorderBrush="#33FFFFFF"
                                            BorderThickness="1"
                                            CornerRadius="8"
                                            MinWidth="{TemplateBinding ActualWidth}"
                                            MaxHeight="200">
                                        <ScrollViewer Margin="5">
                                            <StackPanel IsItemsHost="True"/>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- CheckBox样式 -->
        <Style x:Key="ModernCheckBox" TargetType="CheckBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#D1D5DB"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name="checkBorder"
                                    Width="20"
                                    Height="20"
                                    Background="#1AFFFFFF"
                                    BorderBrush="#33FFFFFF"
                                    BorderThickness="1"
                                    CornerRadius="4"
                                    Margin="0,0,10,0">
                                <Path x:Name="checkMark"
                                      Data="M 3 10 L 8 15 L 17 5"
                                      Stroke="#3B82F6"
                                      StrokeThickness="2.5"
                                      Visibility="Collapsed"
                                      Stretch="None"/>
                            </Border>
                            <ContentPresenter VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="checkMark" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="checkBorder" Property="BorderBrush" Value="#3B82F6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

<!-- 进度条样式 -->
        <Style x:Key="ModernProgressBar" TargetType="ProgressBar">
            <Setter Property="Height" Value="6"/>
            <Setter Property="Minimum" Value="0"/>
            <Setter Property="Maximum" Value="100"/>
            <Setter Property="Background" Value="#1AFFFFFF"/>
            <Setter Property="Foreground" Value="#3B82F6"/>
            <Setter Property="IsIndeterminate" Value="False"/>
        </Style>
    </Window.Resources>

    <!-- 主容器 -->
    <Border CornerRadius="16,16,0,0" Margin="20,20,20,0">
        <Border.Effect>
            <DropShadowEffect Color="Black" Direction="315" ShadowDepth="10" Opacity="0.5" BlurRadius="25"/>
        </Border.Effect>

        <Grid Background="#1E1E2E">
            <!-- 背景图片 -->
            <Image x:Name="BackgroundImage" Stretch="UniformToFill" Opacity="0.08"/>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="60"/>
                    <RowDefinition Height="310"/>
                    <RowDefinition Height="100"/>
                    <RowDefinition Height="80"/>
                </Grid.RowDefinitions>

                <!-- Row 0: 标题栏 -->
                <Border Grid.Row="0" Background="#181825">
                    <Grid Margin="20,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- 标题 -->
                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <materialDesign:PackIcon Kind="Download" Width="22" Height="22" 
                                                    Foreground="#3B82F6" Margin="0,0,10,0"/>
                            <TextBlock x:Name="txtAnnouncementTitle" Text="直链下载" 
                                      FontSize="18" FontWeight="SemiBold" Foreground="White"/>
                        </StackPanel>

                        <!-- 窗口控制按钮 -->
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <Button x:Name="btnMinimize" Click="BtnMinimize_Click"
                                    Style="{StaticResource IconButton}" Margin="0,0,8,0">
                                <materialDesign:PackIcon Kind="Minus" Width="16" Height="16"/>
                            </Button>
                            <Button x:Name="btnMaximize" Click="BtnMaximize_Click"
                                    Style="{StaticResource IconButton}" Margin="0,0,8,0">
                                <materialDesign:PackIcon Kind="CheckboxMultipleBlankOutline" Width="14" Height="14"/>
                            </Button>
                            <Button x:Name="btnClose" Click="BtnClose_Click"
                                    Style="{StaticResource IconButton}" Foreground="#EF4444">
                                <materialDesign:PackIcon Kind="Close" Width="16" Height="16"/>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Row 1: 公告区域 -->
                <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="25,20">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" 
                                 HorizontalScrollBarVisibility="Disabled"
                                 Padding="20">
                        <StackPanel x:Name="txtAnnouncementContent">
                            <!-- 动态加载公告内容 -->
                        </StackPanel>
                    </ScrollViewer>
                </Border>

                <!-- Row 2: 进度区域 -->
                <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="25,0,25,15">
                    <Grid Margin="20">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- 进度信息 -->
                        <Grid Grid.Row="0" Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                <TextBlock x:Name="lblProgress" Text="准备就绪" 
                                          FontSize="15" FontWeight="Medium" Foreground="White"/>
                                <TextBlock x:Name="lblSpeed" Text="" 
                                          FontSize="13" Foreground="#9CA3AF" Margin="0,4,0,0"/>
                            </StackPanel>

                            <!-- 暂停/继续按钮 -->
                            <Button x:Name="btnPauseResume" Grid.Column="1"
                                   Click="BtnPauseResume_Click"
                                   Width="40" Height="40"
                                   Background="#3B82F6"
                                   IsEnabled="False">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border"
                                                Background="{TemplateBinding Background}" 
                                                CornerRadius="8">
                                            <Border.Effect>
                                                <DropShadowEffect Color="#3B82F6" Direction="315" ShadowDepth="2" Opacity="0.4" BlurRadius="8"/>
                                            </Border.Effect>
                                            <materialDesign:PackIcon x:Name="icon" Kind="Pause" Width="18" Height="18" 
                                                                    Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#2563EB"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#1D4ED8"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </Grid>

                        <!-- 进度条 -->
                        <ProgressBar x:Name="progressBar" Grid.Row="1"
                                    Value="0" Maximum="100"
                                    Style="{StaticResource ModernProgressBar}"/>
                    </Grid>
                </Border>

                <!-- Row 3: 控制区域 -->
                <Border Grid.Row="3" Background="#181825">
                    <Grid Margin="25,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- 自动解压选项 -->
                        <CheckBox x:Name="chkAutoExtract" Grid.Column="0"
                                 Content="自动解压 ZIP"
                                 IsChecked="True"
                                 Style="{StaticResource ModernCheckBox}"
                                 VerticalAlignment="Center"
                                 Margin="0,0,20,0"/>

                        <!-- 下载项目选择 -->
                        <ComboBox x:Name="cmbDownloadLinks" Grid.Column="1"
                                 SelectionChanged="CmbDownloadLinks_SelectionChanged"
                                 Style="{StaticResource ModernComboBox}"
                                 VerticalAlignment="Center"
                                 Margin="0,0,15,0"/>

                        <!-- 开始下载按钮 -->
                        <Button x:Name="btnDownload" Grid.Column="2"
                               Click="BtnDownload_Click"
                               Content="开始下载"
                               Style="{StaticResource PrimaryButton}"/>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
